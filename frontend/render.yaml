# Konfiguracja Render dla Frontendu (Static Site)
services:
  - type: web
    name: st-krakos-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

